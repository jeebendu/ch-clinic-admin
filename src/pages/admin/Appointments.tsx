
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Appointments = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Appointments Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">Appointments management interface would be displayed here.</p>
      </CardContent>
    </Card>
  );
};

export default Appointments;
