
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Patients = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Patients Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">Patients management interface would be displayed here.</p>
      </CardContent>
    </Card>
  );
};

export default Patients;
