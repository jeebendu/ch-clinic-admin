
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Records = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Medical Records</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">Medical records management interface would be displayed here.</p>
      </CardContent>
    </Card>
  );
};

export default Records;
