
import { Route, Routes } from "react-router-dom";

const SalesRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div>Sales Dashboard</div>} />
    </Routes>
  );
};

export default SalesRoutes;
