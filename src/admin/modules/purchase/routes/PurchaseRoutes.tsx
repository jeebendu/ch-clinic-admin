
import { Route, Routes } from "react-router-dom";

const PurchaseRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div>Purchase Dashboard</div>} />
    </Routes>
  );
};

export default PurchaseRoutes;
