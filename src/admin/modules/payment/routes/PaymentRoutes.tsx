
import { Route, Routes } from "react-router-dom";

const PaymentRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div>Payment Dashboard</div>} />
    </Routes>
  );
};

export default PaymentRoutes;
