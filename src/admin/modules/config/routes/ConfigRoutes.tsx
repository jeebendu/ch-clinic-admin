
import { Route, Routes } from "react-router-dom";

const ConfigRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div>Config Dashboard</div>} />
    </Routes>
  );
};

export default ConfigRoutes;
